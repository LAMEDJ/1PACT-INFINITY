# ============================================
# 1PACT Backend – Clés et configuration
# Copie en .env et remplis. Ne jamais commiter .env.
# ============================================

# --- Obligatoire ---
PORT=3000
JWT_SECRET=change-moi-en-production

# En production : génère un secret fort :
# node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"

# --- CORS ---
CORS_ORIGIN=http://localhost:5173

# --- Supabase (recommandé) ---
# Settings > API > Project URL + service_role (secret)
SUPABASE_URL=https://TON_PROJECT_REF.supabase.co
SUPABASE_SERVICE_ROLE_KEY=ta_cle_service_role

# --- Optionnel : script fix Impact ---
# Settings > Database > Connection string > URI (remplace [YOUR-PASSWORD])
# DATABASE_URL=postgresql://postgres.[ref]:MDP@...pooler.supabase.com:6543/postgres
