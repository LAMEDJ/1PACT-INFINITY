# ============================================
# 1PACT Frontend – Clés (uniquement publiques)
# Copie en .env ou .env.local. Ne jamais commiter.
# ============================================

# API backend (en prod, mets l'URL réelle)
VITE_API_URL=http://localhost:3000/api

# Supabase : uniquement URL + clé anon (publique)
# Jamais la service_role ici (exposée au build)
VITE_SUPABASE_URL=https://TON_PROJECT_REF.supabase.co
VITE_SUPABASE_ANON_KEY=ta_cle_anon_publique
